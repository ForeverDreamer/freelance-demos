# sample_data

Drop synthetic files here to see the organizer react in real time.

Paired with the default `rules.example.yaml` you should see routing logs
on stdout and files appearing in `./routed/<category>/` relative to the
CWD where you started `organizer.py`.

Do not put real client data here.
